name: Github Action Test
run-name: ${{ github.actor }} started Deploy with gh-pages 🚀
on:
  push:
    branches:
      - feat/test-build-main

  # pull_request:
  #   branches: 
  #     - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: TESTE ENV
      run: |
        echo ${{ github.repository }}
        echo ${{ env.OPEN }}

